apiVersion: apps/v1
kind: Deployment
metadata:
  name: bsc-block-transaction-ingestion
  namespace: ingestion
  labels:
    app: block-transaction-ingestion
    chain: bsc
  annotations:
    reloader.stakater.com/auto: "true"
    
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: block-transaction-ingestion
      chain: bsc
  template:
    metadata:
      labels:
        app: block-transaction-ingestion
        chain: bsc
    spec:
      restartPolicy: Always
      terminationGracePeriodSeconds: 1
      containers:
        - name: ingestion-pipeline-otel
          image: ghcr.io/chainlake/rpcstream:v0.1.6
          imagePullPolicy: IfNotPresent
          command: ["python", "block_pipeline_realtime.py"]

          # -----------------------------
          # Ingest runtime variables
          # -----------------------------
          env:
                         
            - name: POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid

            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          # -----------------------------
          # Metrics endpoint
          # -----------------------------

          # -----------------------------
          # (Optional but recommended)
          # Resource limits
          # -----------------------------
          resources:
            requests:
              cpu: "200m"
              memory: "64Mi"
            limits:
              cpu: "400m"
              memory: "128Mi"